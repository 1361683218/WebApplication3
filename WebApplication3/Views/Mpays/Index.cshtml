@model IEnumerable<WebApplication3.Models.Mpay>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Mlevel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Mpay1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PidNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Mlevel)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Mpay1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PidNavigation.Pid)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Mid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Mid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Mid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
